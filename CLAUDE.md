# RedPear

**Read [PROJECT.md](PROJECT.md) first.** It is the single source of truth for this
project: current progress, stack, design tokens, the Figma node map, conventions and
gotchas, open decisions, and the session log.

This file exists only because Claude Code auto-loads `CLAUDE.md` and nothing else. Keep
all real content in `PROJECT.md`, and keep this pointer so a fresh session finds it.
